name: calculator
description: A calculator app built with Flutter.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  math_expressions: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
